{
  "name": "sd-snapshot-tools",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Headless snapshot tools: sd-animation-snapshot (per-pause grid) and sd-ppt-snapshot (per-slide grid). Used by Claude (and humans) for visual feedback.",
  "dependencies": {
    "playwright": "^1.49.0",
    "sharp": "^0.34.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["sharp"]
  }
}
