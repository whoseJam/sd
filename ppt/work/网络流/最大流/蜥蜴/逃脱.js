import * as sd from "@/sd";

const svg = sd.svg();
const n = 5;
const m = 8;
const grid = new sd.Grid(svg).n(n).m(m).startN(1).startM(1);
const current = [];

sd.init(() => {});

sd.main(async () => {});
