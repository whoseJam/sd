// Per-deck entry. The framework + include-html walker live in the shared
// dist/webslides.js bundle loaded by template.html, so each deck only needs
// HtmlWebpackPlugin to emit index.html with the right <%= base %>.
export {};
